
#include <stdio.h>
int main() {

	int x;
	int y;

	printf("Enter two integers:\n");
	scanf("%d, %d", &x, &y);

	if(x > y)
printf ("= %d is the greater value.", x);


	if(x < y);
printf ("= %d is the greater value.", y);

	return 0;
}
